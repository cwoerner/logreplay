<?xml version="1.0" encoding="UTF-8"?>
<project name="pipeline dependencies">
	<description>
		include file defining all the dependencies for webleads
	</description>

	<import file="${basedir}/dependency-tasks.xml" />

	<!-- dmd-utils library 
	<property name="common.dmd-utils.dir" value="${common.dir}/dmd-utils-${dep.dmd-utils.version}" />
	<property name="common.dmd-utils.lib.dir" value="${common.dmd-utils.dir}/lib" />
	<property name="common.dmd-utils.release.dir" value="${common.dmd-utils.dir}/release" />
	<property name="common.dmd-utils.release.jar" value="${common.dmd-utils.release.dir}/dmd-utils.jar" />
	<path id="classpath.dmd-utils">
          <fileset dir="${common.dmd-utils.lib.dir}" includes="**/*.jar" />
	</path>	
	<target name="package.dmd-utils" depends="config, prepare" description="creates the dmd-utils dependency">
	  <packagedep dependencyName="dmd-utils" dependencyDir="${common.dmd-utils.dir}" />
	</target>
	<target name="clean.dmd-utils" depends="config" description="cleans the dmd-utils package">
	  <cleandep  dependencyName="dmd-utils" dependencyDir="${common.dmd-utils.dir}" />
	</target>
	<target name="test.dmd-utils" depends="config" description="runs the dmd-utils tests">
		<testdep dependencyName="dmd-utils" dependencyDir="${common.dmd-utils.dir}" />
	</target>
	//-->

	<target name="codeps" description="Checkout all the dependencies into the components dir">
	  <!-- add dependencies to checkout here, configure using buid.dependencies.properties or ${HOME}/build.dependencies-local.properties
	       <codep dependencyName="dmd-utils" repositoryUrl="${svn.url}/${dep.dmd-utils.co.svn}" dependencyDir="${common.dmd-utils.dir}" repositoryUser="${svn.user}" repositoryPassword="${svn.password}" />
	       //-->
	</target>
	
	<!-- define all dependency artifacts //-->
	<path id="classpath.compile.dependencies">
	  <!--<fileset dir="${common.dmd-utils.release.dir}" includes="*.jar" /> //-->
	</path>
	
	<!--
	<target name="package.dependencies" depends="codeps, package.dmd-utils" description="build all the project dependencies" /> 
	<target name="clean.dependencies" depends="codeps, clean.dmd-utils" description="cleans all the project dependencies" />
	<target name="test.dependencies" depends="codeps, test.dmd-utils" description="tests all the project dependencies" /> 
	//-->
	<target name="package.dependencies" depends="codeps" description="build all the project dependencies" /> 
	<target name="clean.dependencies" depends="codeps" description="cleans all the project dependencies" />
	<target name="test.dependencies" depends="codeps" description="tests all the project dependencies" /> 

</project>
