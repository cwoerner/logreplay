README for haproxy-log-replay-tool

BUILDING


INSTALLATION


RUNNING

Requires installation of libjnotify.so in /usr/lib

(for windows, jnotify.dll needs to be copied into a directory referenced by the PATH env var)